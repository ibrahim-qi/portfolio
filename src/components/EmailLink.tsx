"use client";

// Email link removed - social links are now prominently displayed in Hero section
// Keeping this file to avoid breaking imports, but rendering nothing

const EmailLink = () => {
  return null;
};

export default EmailLink;
